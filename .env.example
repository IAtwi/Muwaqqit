# Google OAuth credentials for a "Desktop app" OAuth client.
# The Sluice client works as is; see README.md -> Google Cloud setup.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Both are printed by `npm run auth`. Sign in there with the account whose
# calendar should receive the prayer events.
GOOGLE_REFRESH_TOKEN=
GOOGLE_CALENDAR_ID=
